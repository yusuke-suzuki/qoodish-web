import { CLOSE_REQUEST_NOTIFICATION_DIALOG } from '../actionTypes';

const closeRequestNotificationDialog = () => {
  return {
    type: CLOSE_REQUEST_NOTIFICATION_DIALOG
  };
};

export default closeRequestNotificationDialog;
