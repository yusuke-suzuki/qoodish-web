import { OPEN_REQUEST_NOTIFICATION_DIALOG } from '../actionTypes';

const openRequestNotificationDialog = () => {
  return {
    type: OPEN_REQUEST_NOTIFICATION_DIALOG
  };
};

export default openRequestNotificationDialog;
