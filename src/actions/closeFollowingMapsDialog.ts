import { CLOSE_FOLLOWING_MAPS_DIALOG } from '../actionTypes';

const closeFollowingMapsDialog = () => {
  return {
    type: CLOSE_FOLLOWING_MAPS_DIALOG
  };
};

export default closeFollowingMapsDialog;
