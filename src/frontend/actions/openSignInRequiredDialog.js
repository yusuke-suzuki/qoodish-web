import { OPEN_SIGN_IN_REQUIRED_DIALOG } from '../actionTypes';

const openSignInRequiredDialog = () => {
  return {
    type: OPEN_SIGN_IN_REQUIRED_DIALOG
  };
};

export default openSignInRequiredDialog;
