import { CLOSE_SIGN_IN_REQUIRED_DIALOG } from '../actionTypes';

const closeSignInRequiredDialog = () => {
  return {
    type: CLOSE_SIGN_IN_REQUIRED_DIALOG
  };
};

export default closeSignInRequiredDialog;
