// deepOrange[900], the theme's primary, repeated as a literal for the surfaces
// that cannot read the theme: the manifest, the viewport's theme-color, and
// the two global error documents. Those two render when the app itself has
// failed and are deliberately free of the component library, so they cannot
// reach for the palette either. Keep in step with `Providers.tsx`.
export const BRAND_COLOR = '#bf360c';

export const BRAND_COLOR_CONTRAST = '#ffffff';
